/**
 * @module services
 * API service layer — centralizes all external data fetching and network logic.
 */

export { fetchData } from './api';
export { getWeatherByCity, isWeatherStackError } from './weatherService';
